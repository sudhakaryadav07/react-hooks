const INITIAL = {
    activities: []
}

const activitiesReducer = (state = INITIAL, action) => {
    switch (action.type) {
        // case type:
        //     return {
        //         ...state,

        //     };

        default:
            return state;
    }
}

export default activitiesReducer;