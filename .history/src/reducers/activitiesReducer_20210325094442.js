const INITIAL = {
    activities: []
}

export default activitiesReducer = (state = INITIAL, action) => {
    switch (action.type) {
        // case type:
        //     return {
        //         ...state,

        //     };

        default:
            return state;
    }
}