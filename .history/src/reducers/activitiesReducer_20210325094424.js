const INITIAL = {
    activities: []
}

export default= (state = INITIAL, action) => {
    switch (action.type) {
        // case type:
        //     return {
        //         ...state,

        //     };

        default:
            return state;
    }
}